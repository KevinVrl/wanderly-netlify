/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export", // Permet l'export statique compatible avec App Router
  images: {
    unoptimized: true, // Désactive l'optimisation des images qui bloque l'export
  },
};

module.exports = require('./dist/shared/lib/runtime-config')
